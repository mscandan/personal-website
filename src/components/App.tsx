import React from 'react';
import styled from 'styled-components';

const StyledAppContainer = styled.div`
  width: 100vw;
  height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 22px;
`;

const App: React.FC = () => {
  return <StyledAppContainer>Work in progress</StyledAppContainer>;
};

export default App;
